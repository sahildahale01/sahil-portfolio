# Hero video setup

The supplied hero video is already placed at:

`public/hero-video.mp4`

The hero component references it with:

`/hero-video.mp4`

Do not move the file out of `public/` unless you also update `src/components/Hero.tsx`.

The video is intentionally rendered as hero artwork with soft edge fades rather than as a bordered video card.
